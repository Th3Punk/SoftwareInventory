{
  "*.cs": "bash -c 'command -v dotnet >/dev/null 2>&1 && dotnet format AppInventory.sln --include \"$@\" || echo \"[lint-staged] dotnet not found, skipping format\"' _",
  "*.{ts,tsx}": "cd frontend && npx eslint --fix",
  "**/*.md": "npx markdownlint-cli2 --fix"
}
