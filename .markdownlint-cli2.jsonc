{
  "config": {
    "default": true,
    // Disable line length (Hungarian text + tables naturally exceed 80 chars)
    "MD013": false,
    // Allow duplicate headings in different sections
    "MD024": { "siblings_only": true },
    // Allow raw HTML (Scalar, badges, etc.)
    "MD033": false,
    // Many existing docs use unlabeled code blocks
    "MD040": false
  }
}
